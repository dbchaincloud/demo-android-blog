<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cloud.dbchain.sample">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo_android">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.mnemonic.MnemonicActivity" />
        <activity android:name=".ui.mnemonic.login.LoginActivity" />
        <activity android:name=".ui.user.main.UserMainActivity" />
        <activity android:name=".ui.user.UserInfoActivity" />
        <activity android:name=".ui.blogdetail.BlogDetailActivity" />
        <activity android:name=".ui.edit.EditActivity" />
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>